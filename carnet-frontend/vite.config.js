import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  server: {
    port: 3000,
    host: true, // Local network sharing ke liye open kiya hai
    open: true, // Browser auto-open hoga run karne par
  },
  build: {
    outDir: 'dist',
    sourcemap: false, // Production build ko lightweight rakhne ke liye
    chunkSizeWarningLimit: 1600,
    rollupOptions: {
      output: {
        manualChunks(id) {
          if (id.includes('node_modules')) {
            return id.toString().split('node_modules/')[1].split('/')[0].toString();
          }
        },
      },
    },
  },
});